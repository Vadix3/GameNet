<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vadim.gamenet">

    <application
        android:name="com.vadim.gamenet.MyAppClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlayMate">
        <activity android:name="com.vadim.gamenet.activities.MainActivity" />
        <activity
            android:name="com.vadim.gamenet.activities.LoginSignupActivity"
            android:windowSoftInputMode="adjustPan">
            android:theme="@style/Theme.PlayMate">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>